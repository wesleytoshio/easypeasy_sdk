class Pubspec {
  static Future<void> runAsync(String pubspecDir) async {}
  static Future<void> runIcon(String pubspecDir) async {
    // final yaml = YamlFile();
    // final File file = File('$pubspecDir/pubspec.yaml');
    // final String yamlString = file.readAsStringSync();
    // final Map yamlMap = json.decode(json.encode(loadYaml(yamlString)));
    // yamlMap['flutter_native_splash']['color'] = data.splashColorLight;
    // yamlMap['flutter_native_splash']['color_dark'] = data.splashColorDark;
    // yamlMap['flutter_native_splash']['image'] =
    //     '$assetsFolder/$splashLightName';
    // yamlMap['flutter_native_splash']['image_dark'] =
    //     '$assetsFolder/$splashDarkName';
    // yamlMap['flutter_icons']['image_path'] = '$assetsFolder/$appIconName';
    // String yaml = writer.write(yamlMap);
    // "core/assets.yaml".write(yaml);
  }

  static Future<void> runSplash() async {}
}
